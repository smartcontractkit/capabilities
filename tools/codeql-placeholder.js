"use strict";

// This repository is primarily Go, but org-level CodeQL also runs the
// JavaScript/TypeScript analyzer because package metadata exists at the root.
// Keep one valid first-party JS file so the analyzer has source to process.
module.exports = {};

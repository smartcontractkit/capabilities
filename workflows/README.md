### Using Sample workflows

See the `dev-platform` repo for latest on how to build and run the CLI.

### Compilation

To compile the `fetchtrueusd` workflow, execute the following:

```bash
$ cre workflow compile ./fetchtrueusd/cmd/main.go
```

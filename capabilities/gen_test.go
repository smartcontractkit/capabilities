package capabilities

import "testing"

func Test(t *testing.T) {
	// required so lint knows the package was tested.
	// the exit code from running tests is non-zero if there are none.
	// CI detects that this is a go package.
}

# Consensus Capability

The consensus capability is a DON capability that allows callers to reach a consensus value across a set of nodes where
each node may have observed differing values.  The API allows the user to specify how consensus is reached for an arbitrarily
complex object.

```